[project]
name = "rtl-bug-detection-llm-experiments"
version = "0.1.0"
description = "Experiments related to hardware/RTL bug detection, using LLMs"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "openai>=1.99.7",
    "polars>=1.32.2",
]

[dependency-groups]
dev = [
    "pyright>=1.1.403",
    "pytest>=8.4.1",
    "ruff>=0.12.8",
]
